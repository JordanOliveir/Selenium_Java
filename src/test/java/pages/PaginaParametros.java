package pages;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class PaginaParametros extends BasePage{
    public PaginaParametros(WebDriver navegador) {
        super(navegador);
    }
}
